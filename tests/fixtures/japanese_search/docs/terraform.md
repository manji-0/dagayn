# クロスモジュール解析

`module` ブロックの `source` がローカルパスなら、呼び出し元から
対象ディレクトリへ `IMPORTS_FROM` が付く。インパクト半径は
モジュール境界を越える。

## tfvars の変数代入

変数値ファイルのトップレベル代入は `var.name` ノードになり、
対応する `variable` ブロックへ REFERENCES で繋がる。

## OpenSearch ドメイン検索

インフラ側のドメイン検索はクラスタ運用の話で、GraphStore の
自然言語検索とは別のノードに落とす。
