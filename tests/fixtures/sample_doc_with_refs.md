# API overview

The `BridgePattern` registry is consumed by `_detect_cross_language_bridge`.
Helpers like `_bridge_callee_signature` live in `dagayn.parser`.

<!-- symbol-shape filter rejects these: -->
Run `git status` to check.  Use `--flag` for strict mode.

<!-- length filter rejects these (< 3 chars): -->
Use `if` and `or` freely.

## Subsection

Subsection-attributed ref: `_bridge_first_string_arg`.

<!-- unknown symbol that postprocess should drop: -->
Also `NonexistentSymbolXYZ` is mentioned here.
