# Cross-artifact bridge fixture (Phase 4 analysis)

<!-- dagayn: implemented-by ../bridge_wrapper.py::launch_native -->

This contract section is linked to the Python wrapper that invokes the native
entry point. Analysis tools should treat the `CROSS_ARTIFACT` edge as a
first-class bridge transition.
