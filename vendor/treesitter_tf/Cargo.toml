[package]
name = "tree-sitter-terraform"
version = "0.0.1"
description = "Terraform grammar for tree-sitter"
edition = "2021"
license = "MIT"
readme = "README.md"
keywords = ["tree-sitter", "terraform", "hcl", "parser"]

[lib]
path = "bindings/rust/lib.rs"

[dependencies]
tree-sitter-language = "0.1"

[build-dependencies]
cc = "1"
